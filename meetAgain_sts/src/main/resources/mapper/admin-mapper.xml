<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminMapper">

<select id="selectBoardList" resultType="Report">
	SELECT R.RCID, GTITLE, GBTITLE, R.RCINFO, R.USERID, R.STMT FROM REPORT R
	JOIN GBOARD USING(GBID) JOIN SGROUP USING(GID) ORDER BY STMT, RCID
</select>
<select id="selectBoardTotalContent" resultType="_int">
	SELECT COUNT(*) FROM REPORT
</select>
<select id="selectOneBoard" parameterType="_int" resultType="Report">
	SELECT * FROM REPORT WHERE RCID=#{rcId}
</select>
</mapper>